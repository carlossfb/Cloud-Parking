server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost/db_parking
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
